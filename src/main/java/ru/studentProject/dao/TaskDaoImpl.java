package ru.studentProject.dao;

public class TaskDaoImpl {

    //Session session = HibernateUtil.openSession();

    /*public List<Step> findAllStep(long id) throws ExceptionDao {
        Criteria criteria = session.createCriteria(Step.class);
        Task a = new Task();
        a.setId(id);
        criteria.add(Restrictions.eq("task", a));
        return criteria.list();
    }*/
}
